# =============================================================================
# V Rising Dedicated Server (ARM64) - Environment Configuration
# =============================================================================
# Copy this file to .env and customize as needed.
# Variables are read by docker-compose.yml
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER IDENTITY
# -----------------------------------------------------------------------------
# Name displayed in the server browser
SERVER_NAME=V Rising Docker ARM64

# Name of the world save folder
SAVE_NAME=world_main

# -----------------------------------------------------------------------------
# TIMEZONE
# -----------------------------------------------------------------------------
# Used for log timestamps. Use IANA timezone format.
# Examples: America/New_York, Europe/London, Asia/Tokyo
TZ=America/Sao_Paulo

# -----------------------------------------------------------------------------
# USER/GROUP IDS
# -----------------------------------------------------------------------------
# Match these to your host user if using bind mounts.
# Run 'id' on your host to find your UID/GID.
PUID=1000
PGID=1000

# -----------------------------------------------------------------------------
# BOX64 PERFORMANCE TUNING
# -----------------------------------------------------------------------------
# These settings affect emulation performance and stability.
# Modify with caution - incorrect values may cause crashes.

# STRONGMEM: Enforce x86-style memory ordering
# 0 = Disabled (faster, may cause random crashes)
# 1 = Enabled (recommended, slight performance cost)
# 2 = Forced (most compatible, higher performance cost)
BOX64_DYNAREC_STRONGMEM=1

# BIGBLOCK: Size of translated code blocks
# 0 = Small blocks (more compatible, slower)
# 1 = Medium blocks (recommended balance)
# 2 = Large blocks (faster, may cause issues)
# 3 = Very large blocks (experimental)
BOX64_DYNAREC_BIGBLOCK=1

# FASTNAN: Simplified floating-point NaN handling
# 0 = Accurate (slower)
# 1 = Fast (recommended for games)
BOX64_DYNAREC_FASTNAN=1

# BOX64_LOG: Logging verbosity
# 0 = None (production)
# 1 = Info (recommended)
# 2 = Debug (troubleshooting)
# 3 = Trace (verbose, performance impact)
BOX64_LOG=1

# -----------------------------------------------------------------------------
# WINE CONFIGURATION
# -----------------------------------------------------------------------------
# Debug output level. Use -all for production, or specific channels for debugging.
# Examples:
#   -all           = No debug output (recommended)
#   +all           = All debug output (very verbose)
#   +seh,+loaddll  = Exception handling and DLL loading
WINEDEBUG=-all

# -----------------------------------------------------------------------------
# SCRIPT DEBUG MODE
# -----------------------------------------------------------------------------
# Enable verbose logging in start.sh
# 0 = Normal
# 1 = Debug mode (extra log output)
DEBUG=0
